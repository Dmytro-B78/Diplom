[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: юнит-тесты (быстрые, без внешних зависимостей)
    integration: интеграционные тесты (требуют моки API)
    slow: медленные тесты (backtest и т.д.)
